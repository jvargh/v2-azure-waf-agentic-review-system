# LLM Configuration - Choose ONE option below:

# Option 1: Azure AI Foundry (Recommended)
# AZURE_AI_PROJECT_ENDPOINT=https://your-ai-foundry-name.services.ai.azure.com
# AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4o-mini

# Option 2: Azure OpenAI (Direct)
# AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key
# AZURE_OPENAI_API_VERSION=2024-10-21

# Azure OpenAI Unified Provider - Deployment Configuration
# AZURE_OPENAI_CHAT_FAST_DEPLOYMENT=gpt-4o-mini        # For prompts < 600 tokens (default: gpt-4o-mini)
# AZURE_OPENAI_CHAT_QUALITY_DEPLOYMENT=gpt-4o          # For prompts >= 600 tokens (default: gpt-4o)
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small  # Embedding model (default: text-embedding-3-small)
# AZURE_OPENAI_VISION_DEPLOYMENT=gpt-4o-mini           # Vision-capable model (default: gpt-4o-mini)

# Feature Flags
# LLM_ENABLED=true                                     # Enable LLM chat operations (default: true)
# EMBEDDINGS_ENABLED=true                              # Enable embedding operations (default: true)
# VISION_ENABLED=true                                  # Auto-enable vision when diagrams present (default: true)
# ENABLE_HYBRID_SHADOW=false                           # Attach experimental hybrid scoring (default: false)

# Operational Controls - Resilience & Performance
# AZURE_OPENAI_MAX_CONCURRENT=6                        # Max parallel requests (semaphore limit, default: 6)
# AZURE_OPENAI_RATE_LIMIT_PER_SEC=8                    # Token bucket refill rate per second (default: 8)
# AZURE_OPENAI_RETRY_ATTEMPTS=4                        # Simple retry count (default: 4)
# AZURE_OPENAI_RETRY_BACKOFF=0.75                      # Linear backoff base in seconds (default: 0.75)
# AZURE_OPENAI_FAST_THRESHOLD_TOKENS=600               # Prompt token threshold for fast vs quality (default: 600)

# Managed Identity (Optional - recommended for Azure-hosted apps)
# AZURE_USE_MANAGED_IDENTITY=false                     # Use managed identity instead of API key (default: false)

# Authentication (for Azure CLI)
# AZURE_CLIENT_ID=your_service_principal_client_id
# AZURE_CLIENT_SECRET=your_service_principal_secret
# AZURE_TENANT_ID=your_azure_tenant_id

# MCP Tools Configuration
AZURE_SEARCH_ENDPOINT=your_azure_search_endpoint
AZURE_SEARCH_API_KEY=your_azure_search_api_key

# Agent Configuration
RELIABILITY_AGENT_NAME=ReliabilityFrameworkAgent
SECURITY_AGENT_NAME=SecurityFrameworkAgent
COST_AGENT_NAME=CostOptimizationAgent
OPERATIONAL_AGENT_NAME=OperationalExcellenceAgent
PERFORMANCE_AGENT_NAME=PerformanceEfficiencyAgent

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Cache Settings
CACHE_TTL_HOURS=24
ENABLE_CACHE=true